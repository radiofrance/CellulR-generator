<?php

namespace {{ page_namespace }};

use {{ abstract_page_namespace }}\AbstractPage;

/**
 * Class {{ page_name }}.
 */
class {{ page_name }} extends AbstractPage
{
    /**
     * @return array The array of data
     */
    public function __invoke()
    {
        return array();
    }
}
